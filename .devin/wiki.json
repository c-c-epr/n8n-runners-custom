{
  "repo_notes": [
    {
      "content": "This is a tool that can automatically install the necessary Python and JS packages using requirements.txt and package.json to create a customized version of n8n runners. Its key feature is full compatibility with requirements.txt and package.json. When used with GitHub Actions, it can automatically update n8n runners with each push."
    }
  ]
}
